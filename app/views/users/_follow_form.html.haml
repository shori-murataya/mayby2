#follow_form
  - if current_user.following? @user
    = link_to "フォロー外す", unfollow_user_path(@user), { remote: true, class: "unfollow-btn", method: "post" }
  - else
    = link_to "フォローする", follow_user_path(@user), { remote: true, class: "follow-btn", method: "post" }