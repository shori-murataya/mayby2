.user-show-users-box
  - @user_comments.each do |comment|
    .card.user_comment_index_list
      .card-header.title_header
        = link_to post_path(comment.post_id), { class: "user_comment_title_link" } do
          %h3.user_comment_index_title= comment.post.title 
        - if comment.comment_user?(current_user)
          = render partial: "comments/destroy" , locals: { comment: comment }
      .card-body
        %p= simple_format( comment.content )
      %footer.comment_time= "コメント日時：#{ l comment.created_at }"
